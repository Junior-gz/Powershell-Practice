# Creates a Symbolic Link to a folder
New-Item -ItemType SymbolicLink -Path "insert path" -Target "insert target"

# Creates a Symbolic Link to a file
New-Item -Itemtype SymbolicLink -Path "insert path" -Target "insert target"